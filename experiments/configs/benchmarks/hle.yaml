# experiments/configs/benchmarks/hle.yaml

benchmark:
  name: HLE
  data_path: experiments/benchmarks/HLE/data/hle.json

model:
  solver: gpt-5.2
  judge:  gpt-5.2

evaluation:
  category:    null
  text_only:   True
  limit:       null
  max_workers: 1

output:
  dir:     experiments/results
  verbose: false

# ← experiment 块完全删掉，由 method yaml 提供