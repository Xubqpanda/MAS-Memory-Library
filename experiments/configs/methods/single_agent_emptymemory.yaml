# experiments/configs/methods/single_agent_emptymemory.yaml

experiment:
  name:            single_agent_emptymemory
  memory_method:   empty
  agent_framework: single_agent

memory_config:
  namespace:   hle_empty
  working_dir: experiments/results/memory_store   # 相对仓库根，runner 里会拼 REPO_ROOT

mas_config: {}   # SingleAgentSolver 目前没有额外超参，保留占位